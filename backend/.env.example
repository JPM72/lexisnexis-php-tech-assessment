# Database Configuration
DB_HOST=localhost
DB_NAME=document_search
DB_USER=root
DB_PASS=
DB_PORT=3306

# File Storage
UPLOAD_PATH=backend/storage/uploads
CACHE_PATH=backend/storage/cache
LOG_PATH=backend/storage/logs

# CORS Configuration
CORS_ORIGIN=http://localhost:4200

# File Upload Limits
MAX_FILE_SIZE=10485760  # 10MB in bytes
ALLOWED_FILE_TYPES=text/plain,application/pdf

# Cache Settings
CACHE_TTL=3600  # Cache time to live in seconds
CACHE_CLEANUP_INTERVAL=86400  # 24 hours

# Search Configuration
SEARCH_RESULTS_PER_PAGE=10
MAX_SEARCH_RESULTS=100

# Application Settings
APP_DEBUG=true
APP_ENV=development
APP_TIMEZONE=UTC

# Frontend Configuration
FRONTEND_URL=http://localhost:4200
API_BASE_URL=http://localhost:8000